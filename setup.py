#!/usr/bin/env python

import setuptools

setuptools.setup(
      name='dlra',
      version='0.0',
      description='Code for dictionary-based low-rank approximations (DLRA)',
      author='Jeremy E. Cohen',
      author_email='jeremy.cohen@irisa.fr',
      packages=setuptools.find_packages(),
      license='MIT'
     )
